<Page
    x:Class="Sample.UWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--TitlePanel-->
        <StackPanel Grid.Row="0" Margin="12,17,0,28">
            <TextBlock Text="Universal Windows Platform" Style="{StaticResource BaseTextBlockStyle}"/>
            <TextBlock Text="CrossPlatformLibrary Sample App" Style="{StaticResource TitleTextBlockStyle}"/>
        </StackPanel>

        <!--ContentPanel-->
        <StackPanel Grid.Row="1" Margin="12,17,0,28">
            <Button Content="Throw InvalidOperationException" Click="ButtonOnClick"/>
        </StackPanel>
    </Grid>
</Page>
